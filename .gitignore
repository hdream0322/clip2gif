# Xcode
*.xcodeproj
xcuserdata/
*.xccheckout
*.xcscmblueprint
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift
.build/
build/
*.o
*.d

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# App artifacts
*.gif

# Local release output (DMGs are published via CI, not committed)
dist/

# NOTE: the bundled gifski (Clip2GIF/Resources/bin/gifski) is intentionally
# committed so CI can build without Homebrew. Run scripts/bootstrap.sh locally
# only to refresh it to a newer version.

# OMC / Claude Code internal state (keep .omc/plans/ only)
.omc/state/
.omc/sessions/
.omc/project-memory.json
.claude/
